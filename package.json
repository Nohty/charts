{
  "name": "charts",
  "description": "A simple charting library",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "repository": "git@github.com:Nohty/charts.git",
  "author": "daan hautekiet <daanhautekiet@gmail.com>",
  "license": "GPL-3.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "build:example": "npm -prefix ./example run build",
    "build:example:watch": "npm -prefix ./example run build:watch",
    "build:all": "npm run build && npm run build:example"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
